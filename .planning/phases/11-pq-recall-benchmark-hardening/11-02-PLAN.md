---
phase: 11-pq-recall-benchmark-hardening
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - crates/velesdb-core/benches/pq_recall_benchmark.rs
  - benchmarks/baseline.json
autonomous: true
requirements:
  - PQ-07
gap_closure: true

must_haves:
  truths:
    - "The benchmark asserts recall@10 >= 0.92 for PQ m=8 k=256 with rescore"
    - "The benchmark asserts recall@10 >= 0.92 for OPQ m=8 k=256 with rescore"
    - "The benchmark asserts recall@10 >= 0.92 for PQ m=8 k=256 with oversampling=8"
    - "The benchmark asserts recall@10 >= 0.95 for full-precision baseline"
    - "The benchmark uses a dataset large enough for HNSW to achieve 0.92+ recall ceiling"
    - "baseline.json threshold_recall values match the benchmark assertions"
  artifacts:
    - path: "crates/velesdb-core/benches/pq_recall_benchmark.rs"
      provides: "PQ recall benchmark with 0.92+ thresholds on larger dataset"
      contains: "recall >= 0.92"
    - path: "benchmarks/baseline.json"
      provides: "Baseline entries with 0.92 thresholds for PQ/OPQ/OS8"
      contains: "0.92"
  key_links:
    - from: "crates/velesdb-core/benches/pq_recall_benchmark.rs"
      to: "benchmarks/baseline.json"
      via: "Threshold values must match between assertion and baseline"
      pattern: "threshold_recall.*0.92"
---

<objective>
Close the recall threshold gap: increase dataset size from 5K to 20K vectors so the HNSW recall ceiling rises above 0.92, then restore the 0.92 threshold assertions for PQ/OPQ/oversampling8 variants and 0.95 for full-precision baseline. Update baseline.json to match.

Purpose: The HNSW graph on 5K/128d synthetic data has a recall ceiling of ~0.876, making the PQ-07 requirement (recall@10 >= 92%) unreachable. With 20K vectors, the HNSW graph is denser (M=24 connects to a larger fraction of neighbors), lifting the recall ceiling above 0.92. PQ with rescore matches full-precision recall, so once the HNSW ceiling is above 0.92, PQ rescore will also exceed 0.92.
Output: Updated benchmark file with 0.92+ thresholds and matching baseline.json.
</objective>

<execution_context>
@C:/Users/Nicolas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nicolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pq-recall-benchmark-hardening/11-RESEARCH.md
@.planning/phases/11-pq-recall-benchmark-hardening/11-01-SUMMARY.md
@.planning/phases/11-pq-recall-benchmark-hardening/11-VERIFICATION.md

@crates/velesdb-core/benches/pq_recall_benchmark.rs
@benchmarks/baseline.json

<interfaces>
<!-- Key context from verification gap and 11-01 execution -->

Current benchmark constants (line 20-24):
```rust
const DIMENSION: usize = 128;
const NUM_VECTORS: usize = 5_000;   // TOO SMALL -- HNSW ceiling ~0.876
const NUM_CLUSTERS: usize = 10;
const NUM_QUERIES: usize = 50;
const K: usize = 10;
```

Current recall assertions (lines 257, 274, 307, 340):
```rust
assert!(recall >= 0.80, ...)  // Should be 0.92 for PQ/OPQ/OS8
assert!(recall >= 0.80, ...)  // Should be 0.95 for full precision
```

HNSW params for 128d (from params.rs HnswParams::auto):
- M=24, ef_construction=300 (for dim 0..=256)
- search_ids uses SearchQuality::Balanced -> ef_search=128

With 20K vectors and M=24, ef_construction=300, ef_search=128:
- Each node has 24 bidirectional links in a 20K-node graph
- Graph connectivity ratio is much higher than at 5K
- HNSW recall ceiling should be well above 0.92 for k=10

RaBitQ and no-rescore thresholds stay at 0.80 and 0.20 respectively (these are quantization-quality-limited, not HNSW-limited).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase dataset size and restore 0.92 recall thresholds</name>
  <files>crates/velesdb-core/benches/pq_recall_benchmark.rs, benchmarks/baseline.json</files>
  <action>
In `crates/velesdb-core/benches/pq_recall_benchmark.rs`:

1. Change `NUM_VECTORS` from `5_000` to `20_000`. This gives the HNSW graph 4x more nodes, significantly improving graph connectivity and lifting the recall ceiling above 0.92.

2. Optionally increase `NUM_CLUSTERS` from `10` to `20` to maintain cluster diversity proportional to dataset size.

3. Update recall threshold assertions for the 4 variants that were lowered from plan spec:

   a. `pq_recall_m8_k256_rescore` (line ~257): change `recall >= 0.80` to `recall >= 0.92`. Update the comment to remove the "HNSW ceiling" note and replace with: "PQ-07 contract: recall@10 >= 0.92 for m=8 k=256 with rescore"

   b. `full_precision_baseline` (line ~274): change `recall >= 0.80` to `recall >= 0.95`. Update the comment to: "Full precision HNSW baseline must exceed PQ threshold"

   c. `opq_recall_m8_k256_rescore` (line ~307): change `recall >= 0.80` to `recall >= 0.92`. Update the comment to: "OPQ with rescore should match or exceed standard PQ recall"

   d. `pq_recall_m8_k256_oversampling8` (line ~340): change `recall >= 0.80` to `recall >= 0.92`. Update the comment to: "8x oversampling should match or exceed 4x recall"

4. Keep `pq_recall_m8_k256_no_rescore` at `recall >= 0.20` (no-rescore is inherently limited by PQ distance approximation quality, not HNSW graph density).

5. Keep `rabitq_recall_128d` at `recall >= 0.80` (RaBitQ through Collection search uses regular HNSW, recall depends on HNSW graph quality -- with 20K vectors this will improve but 0.80 is a conservative contract for binary quantization).

6. Update the module-level doc comment to mention the 20K dataset size.

In `benchmarks/baseline.json`:

1. Update `pq_recall/pq_recall_m8_k256_rescore`: set `threshold_recall` to `0.92`, update `notes` to "PQ m=8 k=256, rescore=4x, 20K/128d dataset"

2. Update `pq_recall/full_precision_baseline`: set `threshold_recall` to `0.95`, update `notes` to "Full precision HNSW reference, 20K/128d dataset"

3. Update `pq_recall/opq_recall_m8_k256_rescore`: set `threshold_recall` to `0.92`, update `notes` to "OPQ with PCA rotation, 20K/128d dataset"

4. Update `pq_recall/pq_recall_m8_k256_oversampling8`: set `threshold_recall` to `0.92`, update `notes` to "PQ with 8x oversampling, 20K/128d dataset"

5. Keep `pq_recall/pq_recall_m8_k256_no_rescore` at `threshold_recall: 0.20` (unchanged).

6. Keep `pq_recall/rabitq_recall_128d` at `threshold_recall: 0.80` (unchanged).

7. Update the top-level `notes` array: replace the "Recall thresholds set to 0.80" note with "PQ recall thresholds restored to 0.92 (PQ-07 contract) after increasing dataset from 5K to 20K vectors"

8. Update `description` field to mention 0.92 thresholds.

IMPORTANT: If the benchmark fails with 0.92 threshold after increasing to 20K vectors, try increasing to 50K vectors. The goal is to find a dataset size where HNSW recall ceiling exceeds 0.92, then set that as the benchmark constant. Document the chosen size in the commit message.

IMPORTANT: The benchmark will take longer with 20K vectors (PQ training + HNSW construction). This is expected and acceptable for a recall accuracy benchmark (not a latency benchmark). The `sample_size(10)` and `measurement_time(5s)` settings may need adjustment -- reduce `sample_size` to `10` (already set) and keep `measurement_time` at `5` seconds.
  </action>
  <verify>
    <automated>cargo bench -p velesdb-core --bench pq_recall_benchmark -- --noplot 2>&amp;1 | tail -30</automated>
  </verify>
  <done>All 6 benchmark functions pass with PQ/OPQ/OS8 at >= 0.92 recall, full precision at >= 0.95, RaBitQ at >= 0.80, no-rescore at >= 0.20. baseline.json thresholds match assertions. PQ-07 requirement fully satisfied.</done>
</task>

</tasks>

<verification>
1. `cargo bench -p velesdb-core --bench pq_recall_benchmark -- --noplot` runs all 6 benchmark functions without assertion failures
2. PQ rescore, OPQ rescore, and oversampling8 all assert recall >= 0.92
3. Full precision baseline asserts recall >= 0.95
4. RaBitQ asserts recall >= 0.80, no-rescore asserts recall >= 0.20
5. `benchmarks/baseline.json` has threshold_recall 0.92 for PQ/OPQ/OS8 entries
6. `cargo clippy -p velesdb-core --bench pq_recall_benchmark --features persistence,gpu,update-check -- -D warnings` passes
</verification>

<success_criteria>
- PQ-07 fully satisfied: recall@10 >= 0.92 for m=8 k=256 with rescore, enforced in Criterion benchmark
- ROADMAP Phase 11 success criterion 1 met: benchmark asserts recall@10 >= 0.92
- baseline.json thresholds match the benchmark assertions
- All 6 benchmark variants pass their respective thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/11-pq-recall-benchmark-hardening/11-02-SUMMARY.md`
</output>
