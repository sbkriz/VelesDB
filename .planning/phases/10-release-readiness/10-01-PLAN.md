---
phase: 10-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/velesdb-server/Cargo.toml
  - crates/velesdb-python/Cargo.toml
  - crates/velesdb-python/pyproject.toml
  - crates/velesdb-cli/Cargo.toml
  - crates/velesdb-migrate/Cargo.toml
  - crates/velesdb-mobile/Cargo.toml
  - crates/velesdb-wasm/Cargo.toml
  - crates/tauri-plugin-velesdb/Cargo.toml
  - crates/tauri-plugin-velesdb/guest-js/package.json
  - sdks/typescript/package.json
  - integrations/langchain/pyproject.toml
  - integrations/llamaindex/pyproject.toml
  - demos/rag-pdf-demo/pyproject.toml
autonomous: true
requirements:
  - REL-01
  - REL-05

must_haves:
  truths:
    - "All workspace crates report version 1.5.0"
    - "cargo publish --dry-run succeeds for all 6 publishable crates"
    - "velesdb-python and velesdb-wasm have publish = false in Cargo.toml"
    - "All inter-crate velesdb-core dependency versions are 1.5.0"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace version 1.5.0"
      contains: 'version = "1.5.0"'
    - path: "crates/velesdb-python/Cargo.toml"
      provides: "publish = false guard"
      contains: "publish = false"
    - path: "crates/velesdb-wasm/Cargo.toml"
      provides: "publish = false guard"
      contains: "publish = false"
  key_links:
    - from: "Cargo.toml"
      to: "crates/*/Cargo.toml"
      via: "workspace.package.version inheritance"
      pattern: 'version\.workspace\s*=\s*true'
    - from: "crates/velesdb-server/Cargo.toml"
      to: "crates/velesdb-core"
      via: "path + version dependency"
      pattern: 'velesdb-core.*version = "1.5.0"'
---

<objective>
Bump all VelesDB workspace versions from 1.4.5 to 1.5.0 and validate crates.io publish readiness.

Purpose: REL-01 requires all crates at 1.5.0 published to crates.io in dependency order. This plan performs the atomic version bump across 14+ files, adds safety guards (publish = false) for non-crates.io packages, and validates with cargo publish --dry-run.
Output: All version files at 1.5.0, dry-run verified, ready for tag-push release.
</objective>

<execution_context>
@C:/Users/Nicolas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nicolas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-release-readiness/10-RESEARCH.md

@scripts/bump-version.ps1
@.github/workflows/release.yml
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic version bump 1.4.5 to 1.5.0 + publish guards</name>
  <files>Cargo.toml, crates/velesdb-server/Cargo.toml, crates/velesdb-python/Cargo.toml, crates/velesdb-python/pyproject.toml, crates/velesdb-cli/Cargo.toml, crates/velesdb-migrate/Cargo.toml, crates/velesdb-mobile/Cargo.toml, crates/velesdb-wasm/Cargo.toml, crates/tauri-plugin-velesdb/Cargo.toml, crates/tauri-plugin-velesdb/guest-js/package.json, sdks/typescript/package.json, integrations/langchain/pyproject.toml, integrations/llamaindex/pyproject.toml, demos/rag-pdf-demo/pyproject.toml</files>
  <action>
    1. Run `powershell -File scripts/bump-version.ps1 -Version "1.5.0"` to bump all 14+ version locations atomically. The script handles workspace Cargo.toml, inter-crate deps, package.json files, pyproject.toml files.

    2. After the script runs, verify by running `powershell -File scripts/bump-version.ps1 -Version "1.5.0" -DryRun` -- all entries should show "Already at 1.5.0".

    3. Add `publish = false` to `[package]` section of:
       - `crates/velesdb-python/Cargo.toml` (PyPI only, not crates.io)
       - `crates/velesdb-wasm/Cargo.toml` (npm only, not crates.io)
       This prevents accidental `cargo publish` for packages that belong on PyPI/npm, not crates.io. The existing release.yml CRATES_TO_PUBLISH list already excludes them, but belt-and-suspenders.

    4. Run `cargo check --workspace` to verify all version references resolve correctly after the bump.

    Note: The WASM pkg/package.json may not exist on disk (it is generated by wasm-pack build). If bump-version.ps1 reports "File not found" for that entry, that is expected and not an error -- the release.yml patches it post-build with a node -e script.
  </action>
  <verify>
    <automated>cargo check --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>All workspace Cargo.toml files show version 1.5.0 or version.workspace = true. Inter-crate velesdb-core deps show version = "1.5.0". Python and TypeScript manifests show 1.5.0. velesdb-python and velesdb-wasm Cargo.toml have publish = false.</done>
</task>

<task type="auto">
  <name>Task 2: Cargo publish dry-run validation for all 6 crates</name>
  <files></files>
  <action>
    Run cargo publish --dry-run for each of the 6 crates that publish to crates.io, in dependency order:

    ```bash
    cargo publish --dry-run -p velesdb-core
    cargo publish --dry-run -p velesdb-server
    cargo publish --dry-run -p velesdb-cli
    cargo publish --dry-run -p velesdb-migrate
    cargo publish --dry-run -p velesdb-mobile
    cargo publish --dry-run -p tauri-plugin-velesdb
    ```

    Each must succeed without errors. Common issues to watch for:
    - Missing version bump in inter-crate deps (should be caught by Task 1)
    - Files excluded by .gitignore that are needed for the crate
    - Missing `license` or `description` fields in Cargo.toml

    If any dry-run fails, diagnose and fix the root cause. The most likely issue is a version mismatch in inter-crate dependencies that bump-version.ps1 missed, or a Cargo.toml metadata field requirement.

    Also verify velesdb-python and velesdb-wasm are NOT publishable:
    ```bash
    cargo publish --dry-run -p velesdb-python 2>&1 | grep -q "publish = false" && echo "OK: velesdb-python blocked"
    cargo publish --dry-run -p velesdb-wasm 2>&1 | grep -q "publish = false" && echo "OK: velesdb-wasm blocked"
    ```
  </action>
  <verify>
    <automated>cargo publish --dry-run -p velesdb-core 2>&1 | tail -3 && cargo publish --dry-run -p velesdb-server 2>&1 | tail -3</automated>
  </verify>
  <done>All 6 publishable crates pass cargo publish --dry-run. velesdb-python and velesdb-wasm are blocked by publish = false. No version mismatch or metadata errors.</done>
</task>

</tasks>

<verification>
- `grep -r 'version = "1.5.0"' Cargo.toml` shows workspace version
- `grep 'publish = false' crates/velesdb-python/Cargo.toml crates/velesdb-wasm/Cargo.toml` shows both guarded
- `cargo publish --dry-run -p velesdb-core` exits 0
- `cargo check --workspace` exits 0
</verification>

<success_criteria>
- All workspace crates at version 1.5.0
- All 6 publishable crates pass cargo publish --dry-run
- velesdb-python and velesdb-wasm guarded with publish = false
- cargo check --workspace passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-release-readiness/10-01-SUMMARY.md`
</output>
